[workspace]
members = [
  "hcctl",
  "healthchecks",
  "monitor",
]

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"

[workspace.dependencies]
clap = { version = "4.3.1", features = [ "color", "deprecated", "derive" ] }

[workspace.package]
version = "1.0.0"
name = "healthchecks-rs"
